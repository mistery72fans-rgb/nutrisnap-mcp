services:
  - type: web
    name: nutrisnap-mcp
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: SECRET_KEY
        value: nutrisnap_secret_2026
